apiVersion: v1
kind: Service
metadata:
  name: hello-samplekuber
  labels:
    app: hello-samplekuber
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: hello-samplekuber
    tier: hello-samplekuber
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-samplekuber
  labels:
    app: hello-samplekub
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: hello-samplekub
        tier: hello-samplekub
    spec:
      containers:
      - image: 127.0.0.1:30400/hello-samplekuber:latest
        name: hello-samplekuber
        ports:
        - containerPort: 80
          name: hello-samplekuber